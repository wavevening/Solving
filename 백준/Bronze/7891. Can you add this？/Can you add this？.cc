#include <iostream>
#include <cmath>

using namespace std;
int main() {
	int co;
	cin >> co;
	for (int i = 0; i < co; i++) {
		long long N, M;
		cin >> N >> M;
		cout << N + M << endl;
	}

	return 0;
}